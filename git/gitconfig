[user]
	name = chriszarate
	email = chris@zarate.org
[core]
	excludesfile = ~/.gitignore_global
	editor = vim +start
	compression = 0
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[alias]
	ahead = log --stat --branches --not --remotes
	amend = commit --amend
[color]
	ui = true
[pack]
	windowMemory = 10m
	packSizeLimit = 20m
